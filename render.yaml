services:
  - type: web
    name: weatherroute
    runtime: docker
    dockerfilePath: ./Dockerfile
    # Render injects PORT automatically; the Dockerfile CMD uses ${PORT:-8000}
    healthCheckPath: /api/cities/search?q=berlin
